<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Wi-Fi Heatmap — Planning & Survey</title>
<style>
  body {
    font-family: "Inter", "Segoe UI", system-ui;
    background: #0e111a;
    color: #e4e8f1;
    margin: 0;
  }
  header {
    background: #171c2a;
    color: white;
    padding: 10px 20px;
    display: flex;
    align-items: center;
    gap: 12px;
    border-bottom: 1px solid #22263c;
  }
  header h1 { font-size: 18px; margin: 0; flex: 1; }
  .pill {
    border: 1px solid #2c3456;
    border-radius: 20px;
    padding: 6px 14px;
    cursor: pointer;
    background: #1a2038;
    color: #d6d8ef;
  }
  .pill.active {
    background: #324089;
    color: #fff;
    box-shadow: 0 0 0 2px #4760ff55;
  }
  .container {
    display: none;
    grid-template-columns: 360px 1fr;
    height: calc(100vh - 60px);
    gap: 10px;
    padding: 10px;
  }
  .container.active { display: grid; }
  .panel {
    background: #15192c;
    border: 1px solid #232947;
    border-radius: 12px;
    padding: 12px;
    overflow-y: auto;
  }
  .section { margin-bottom: 14px; border-bottom: 1px solid #22294755; padding-bottom: 10px; }
  .label { font-size: 13px; color: #a6a9bf; margin-bottom: 4px; }
  input, select, button {
    font-size: 14px;
    border-radius: 8px;
    border: 1px solid #2c3456;
    padding: 7px 9px;
    background: #1a1f35;
    color: #e8ecf5;
  }
  button { cursor: pointer; }
  button:hover { background: #27306b; }
  canvas { border-radius: 8px; border: 1px solid #242a4c; background: #0b0d16; }
  #canvasPlanning, #canvasSurvey {
    width: 100%;
    height: auto;
  }
</style>
</head>
<body>
<header>
  <h1>แป้งโชไวไฟ888</h1>
  <div id="pillPlanning" class="pill active">Planning</div>
  <div id="pillSurvey" class="pill">Survey</div>
</header>

<!-- 🌈 PLANNING PAGE -->
<div class="container active" id="appPlanning">
  <aside class="panel">
    <div class="section">
      <div class="label">1) อัปโหลดแปลน</div>
      <input type="file" id="fileInputPlanning" accept="image/*">
      <button id="btnClearPlanning">ล้าง</button>
    </div>

    <div class="section">
      <div class="label">2) ตั้งสเกล</div>
      <button id="btnScalePlanning">🧭 ตั้งสเกล</button>
      <button id="btnIdlePlanning">🖱️ ปกติ</button>
      <p class="label">สเกลปัจจุบัน: <span id="scaleLabelPlanning">ยังไม่ตั้ง</span></p>
    </div>

    <div class="section">
      <div class="label">3) วาง Access Point</div>
      <input id="apLabelPlanning" placeholder="ชื่อ AP">
      <input id="apP0Planning" type="number" value="-40">
      <select id="apBandPlanning">
        <option value="2.4">2.4 GHz</option>
        <option value="5">5 GHz</option>
      </select>
      <button id="btnAddApPlanning">📡 วาง AP</button>
      <div id="apListPlanning"></div>
    </div>

    <div class="section">
      <div class="label">4) วัสดุ/สิ่งกีดขวาง</div>
      <select id="matTypePlanning">
        <option value="brick">อิฐ</option>
        <option value="concrete">คอนกรีต</option>
        <option value="wood">ไม้</option>
      </select>
      <input id="matAttPlanning" type="number" value="8" step="0.5">
      <button id="btnMatPlanning">🧱 วาดวัสดุ</button>
    </div>

    <div class="section">
      <div class="label">5) เรนเดอร์</div>
      <button id="btnRenderPlanning">🔥 Render Heatmap</button>
      <button id="btnExportPlanning">💾 บันทึกภาพ</button>
    </div>
  </aside>

  <main class="panel">
    <canvas id="canvasPlanning" width="1280" height="800"></canvas>
  </main>
</div>

<!-- 🌈 SURVEY PAGE (โคลนจาก Planning ทั้งหมด) -->
<div class="container" id="appSurvey">
  <aside class="panel">
    <div class="section">
      <div class="label">1) อัปโหลดแปลน</div>
      <input type="file" id="fileInputSurvey" accept="image/*">
      <button id="btnClearSurvey">ล้าง</button>
    </div>

    <div class="section">
      <div class="label">2) ตั้งสเกล</div>
      <button id="btnScaleSurvey">🧭 ตั้งสเกล</button>
      <button id="btnIdleSurvey">🖱️ ปกติ</button>
      <p class="label">สเกลปัจจุบัน: <span id="scaleLabelSurvey">ยังไม่ตั้ง</span></p>
    </div>

    <div class="section">
      <div class="label">3) วาง Access Point</div>
      <input id="apLabelSurvey" placeholder="ชื่อ AP">
      <input id="apP0Survey" type="number" value="-40">
      <select id="apBandSurvey">
        <option value="2.4">2.4 GHz</option>
        <option value="5">5 GHz</option>
      </select>
      <button id="btnAddApSurvey">📡 วาง AP</button>
      <div id="apListSurvey"></div>
    </div>

    <div class="section">
      <div class="label">4) วัสดุ/สิ่งกีดขวาง</div>
      <select id="matTypeSurvey">
        <option value="brick">อิฐ</option>
        <option value="concrete">คอนกรีต</option>
        <option value="wood">ไม้</option>
      </select>
      <input id="matAttSurvey" type="number" value="8" step="0.5">
      <button id="btnMatSurvey">🧱 วาดวัสดุ</button>
    </div>

    <div class="section">
      <div class="label">5) เรนเดอร์</div>
      <button id="btnRenderSurvey">🔥 Render Heatmap</button>
      <button id="btnExportSurvey">💾 บันทึกภาพ</button>
    </div>
  </aside>

  <main class="panel">
    <canvas id="canvasSurvey" width="1280" height="800"></canvas>
  </main>
</div>

<script>
  // ====== สลับโหมด Planning / Survey ======
  const planningBtn = document.getElementById("pillPlanning");
  const surveyBtn   = document.getElementById("pillSurvey");
  const planningApp = document.getElementById("appPlanning");
  const surveyApp   = document.getElementById("appSurvey");

  planningBtn.onclick = () => {
    planningBtn.classList.add("active");
    surveyBtn.classList.remove("active");
    planningApp.classList.add("active");
    surveyApp.classList.remove("active");
  };
  surveyBtn.onclick = () => {
    surveyBtn.classList.add("active");
    planningBtn.classList.remove("active");
    surveyApp.classList.add("active");
    planningApp.classList.remove("active");
  };
</script>

<script src="./app.js"></script>
</body>
</html>
